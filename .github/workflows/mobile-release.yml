name: Mobile Release Pipeline
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      runMatrix:
        description: 'Build dev, staging, and production internal artifacts'
        required: false
        default: 'true'
