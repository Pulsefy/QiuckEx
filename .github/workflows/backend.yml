# This workflow has been superseded by:
#   .github/workflows/backend-ci.yml      (CI: lint, type-check, test)
#   .github/workflows/backend-release.yml (Release: build, migrate, deploy)
#
# This file is kept as a placeholder to avoid broken branch protection rules
# that reference the old workflow name. It can be deleted once branch rules
# are updated to reference the new workflow files.
name: Backend CI (deprecated — see backend-ci.yml)
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use backend-ci.yml and backend-release.yml instead."
