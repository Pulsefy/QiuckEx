{
  "kind": "quickex-wasm-size-baseline-v1",
  "baseline_bytes": 174484,
  "tolerance_pct": 5,
  "updated_at": "2026-08-30",
  "note": "Baseline release WASM size (bytes) for contracts/quickex, built with `cargo build -p quickex --target wasm32v1-none --release`. CI fails when the built WASM exceeds baseline_bytes * (1 + tolerance_pct / 100). Raise baseline_bytes only in a dedicated, reviewable PR that explains the size growth."
}
